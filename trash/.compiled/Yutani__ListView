#!/usr/bin/env bash
# Generated by Trashtalk Compiler (jq) - DO NOT EDIT
# Source: ListView.trash
# Generated: 2026-01-12T22:15:29

__Yutani__ListView__superclass="Yutani::Widget"
__Yutani__ListView__instanceVars="items:"
__Yutani__ListView__classInstanceVars=""
__Yutani__ListView__traits=""
__Yutani__ListView__sourceHash="f069c06a870163908b7c3eeb308774e3638dfec9524fec37c501ded0f390fa40"
__Yutani__ListView__package="Yutani"
__Yutani__ListView__qualifiedName="Yutani::ListView"

__Yutani__ListView__source() {
  cat <<'__TRASHTALK_SOURCE_EOF__'
package: Yutani

ListView subclass: Yutani::Widget
  instanceVars: items:''

  # Create a new ListView with given title
  classMethod: titled: title inSession: session [
    pragma: primitive
    local title="$1" session="$2"
    local instance wid
    instance=$(_generate_instance_id "Yutani__ListView")
    _create_instance "Yutani::ListView" "$instance"
    @ "$instance" setSession: "$session"
    @ "$instance" setTitle: "$title"
    # Create the widget on the server
    wid=$(@ "$session" createList: "$title")
    @ "$instance" setWidgetId: "$wid"
    echo "$instance"
  ]

  # Add an item to the list
  method: addItem: text [
    pragma: primitive
    local text="$1" session wid
    session="$(_ivar session)"
    wid="$(_ivar widgetId)"
    @ "$session" addItem: "$text" to: "$wid"
  ]

  # Add an item with secondary text
  method: addItem: mainText secondary: secondaryText [
    pragma: primitive
    local mainText="$1" secondaryText="$2" session wid
    session="$(_ivar session)"
    wid="$(_ivar widgetId)"
    @ "$session" addItem: "$mainText" secondary: "$secondaryText" to: "$wid"
  ]

  # Clear all items
  method: clear [
    pragma: primitive
    local session wid
    session="$(_ivar session)"
    wid="$(_ivar widgetId)"
    @ "$session" clearList: "$wid"
  ]
__TRASHTALK_SOURCE_EOF__
}

__Yutani__ListView__items() {
  echo "$(_ivar items)"; return
}

__Yutani__ListView__items_() {
  _ivar_set items "$1"
}

__Yutani__ListView__getItems() {
  echo "$(_ivar items)"; return
}

__Yutani__ListView__setItems_() {
  _ivar_set items "$1"
}

__Yutani__ListView__class__titled_inSession_() {
  local title="$1"
  local session="$2"
    local title="$1" session="$2"
    local instance wid
    instance=$(_generate_instance_id "Yutani__ListView")
    _create_instance "Yutani::ListView" "$instance"
    @ "$instance" setSession: "$session"
    @ "$instance" setTitle: "$title"
    # Create the widget on the server
    wid=$(@ "$session" createList: "$title")
    @ "$instance" setWidgetId: "$wid"
    echo "$instance"
}

__Yutani__ListView__addItem_() {
  local text="$1"
    local text="$1" session wid
    session="$(_ivar session)"
    wid="$(_ivar widgetId)"
    @ "$session" addItem: "$text" to: "$wid"
}

__Yutani__ListView__addItem_secondary_() {
  local mainText="$1"
  local secondaryText="$2"
    local mainText="$1" secondaryText="$2" session wid
    session="$(_ivar session)"
    wid="$(_ivar widgetId)"
    @ "$session" addItem: "$mainText" secondary: "$secondaryText" to: "$wid"
}

__Yutani__ListView__clear() {
    local session wid
    session="$(_ivar session)"
    wid="$(_ivar widgetId)"
    @ "$session" clearList: "$wid"
}
