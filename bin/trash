#!/usr/bin/env bash
# Readline, history, and completion live in the REPL's Bash process.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export TRASHTALK_DIR="$(dirname "$SCRIPT_DIR")"
exec "$SCRIPT_DIR/trash-repl" "$@"
